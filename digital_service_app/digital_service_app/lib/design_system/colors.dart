import 'package:flutter/material.dart';

class WebColors {
  static const background = Color(0xFFFFFFFF);
  static const foreground = Color(0xFF0F172A);

  static const primary = Color(0xFF009A44);
  static const primaryForeground = Color(0xFFFFFFFF);

  static const secondary = Color(0xFFFCD116);
  static const secondaryForeground = Color(0xFF1E293B);

  static const destructive = Color(0xFFCE1126);
  static const destructiveForeground = Color(0xFFFFFFFF);

  static const muted = Color(0xFFF8FAFC);
  static const mutedForeground = Color(0xFF64748B);

  static const accent = Color(0xFFF1F5F9);
  static const accentForeground = primary;

  static const border = Color(0xFFE2E8F0);
  static const input = border;
}

